apiVersion: v1
kind: Service
metadata:
  name: {{ include "btc-loki-helm.fullname" . }}
  labels:
    {{- include "btc-loki-helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "btc-loki-helm.selectorLabels" . | nindent 4 }}
